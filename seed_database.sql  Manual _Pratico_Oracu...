-- Criar a tabela para armazenar os dados extraídos
CREATE TABLE IF NOT EXISTS produtos (
    id SERIAL PRIMARY KEY,
    nome_produto TEXT,
    descricao_detalhada TEXT,
    preco_extraido DECIMAL,
    data_captura TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
